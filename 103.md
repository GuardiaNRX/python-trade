1) Freshness-gate (fail-closed)

Problem, który rozwiązuje: kończysz run na niepełnych danych (część tickerów ma D-1), co rozjeżdża PnL i wagi.
Jak działa fix: przed snapshotem porównujemy oczekiwaną ostatnią sesję (wg kalendarza giełdy) z ostatnią datą cen. Jeśli < 95% wszechświata ma dane na tę sesję → STOP + alert i kod wyjścia ≠ 0.

Patch
*** Begin Patch
*** Add File: utils/freshness.py
+import pandas as pd
+import pandas_market_calendars as mcal
+
+def expected_last_session(exchange: str="XNYS", asof: pd.Timestamp|None=None) -> pd.Timestamp:
+    asof = pd.Timestamp.utcnow().normalize() if asof is None else pd.Timestamp(asof)
+    cal = mcal.get_calendar(exchange)
+    sched = cal.schedule(start_date=asof - pd.Timedelta(days=10), end_date=asof + pd.Timedelta(days=1))
+    return pd.Timestamp(sched.index.max()).tz_localize(None)
+
+def coverage_on_date(prices: pd.DataFrame, date: pd.Timestamp) -> float:
+    row = prices.loc[prices.index >= date]
+    if row.empty: return 0.0
+    last = row.iloc[0]
+    ok = last.notna().sum()
+    return ok / len(last)
+
+def assert_fresh(prices: pd.DataFrame, exchange: str="XNYS", min_coverage: float=0.95):
+    exp = expected_last_session(exchange)
+    cov = coverage_on_date(prices, exp)
+    return exp, cov, (cov >= min_coverage)
*** End Patch


Integracja w backtests/report_daily.py (tuż po load_ohlcv i przed snapshotem):

@@
-    prices, volume, high, low = load_ohlcv(uni, ds.get("start"), ds.get("end"), ds.get("source","yfinance"), ds.get("csv_dir","data"))
+    prices, volume, high, low = load_ohlcv(uni, ds.get("start"), ds.get("end"), ds.get("source","yfinance"), ds.get("csv_dir","data"))
+    from utils.freshness import assert_fresh
+    exp, cov, ok = assert_fresh(prices, cfg.get("calendar",{}).get("exchange","XNYS"), min_coverage=cfg.get("data",{}).get("min_coverage",0.95))
+    if not ok:
+        try:
+            post_message(os.environ.get("SLACK_CHANNEL","#alpha-lab"), f":no_entry: Freshness gate FAIL — oczekiwana sesja {exp.date()} pokryta w {cov:.1%}. Przerywam run.")
+        except Exception:
+            pass
+        sys.exit(4)


Dodaj w configs/... (opcjonalnie):

data:
  min_coverage: 0.95

2) Atomic writes + run_id

Problem: pół-pliki i nadpisania przy restartach; trudno zreplikować run.
Fix: helper do atomowych zapisów i run_id (hash czasu + cfg_hash + snapshot_id) w nazwach artefaktów.

Patch
*** Begin Patch
*** Add File: utils/atomic.py
+import os, tempfile
+def atomic_write_bytes(path: str, data: bytes):
+    os.makedirs(os.path.dirname(path), exist_ok=True)
+    with tempfile.NamedTemporaryFile(delete=False, dir=os.path.dirname(path)) as tmp:
+        tmp.write(data); tmp.flush(); os.fsync(tmp.fileno())
+        tmp_path = tmp.name
+    os.replace(tmp_path, path)
+
+def atomic_write_text(path: str, text: str, encoding="utf-8"):
+    atomic_write_bytes(path, text.encode(encoding))
*** End Patch
*** Add File: utils/run_id.py
+import time, hashlib
+def make_run_id(cfg_hash: str, snapshot_id: str) -> str:
+    ts = str(int(time.time()))
+    base = f"{ts}_{cfg_hash[:8]}_{snapshot_id[:8]}"
+    return hashlib.sha1(base.encode()).hexdigest()[:12]
*** End Patch


Modyfikacja report_daily.py (nazywanie plików i zapis md/PNG):

@@
-    snap_meta = save_snapshot(prices, volume, high, low)
+    snap_meta = save_snapshot(prices, volume, high, low)
+    from utils.ledger import cfg_hash
+    from utils.run_id import make_run_id
+    runid = make_run_id(cfg_hash(cfg), snap_meta["snapshot_id"])
@@
-    eq_path = os.path.join(out_dir, f"{name}_equity.png")
-    hist_path = os.path.join(out_dir, f"{name}_hist.png")
-    ric_path = os.path.join(out_dir, f"{name}_rolling_ic.png")
+    eq_path = os.path.join(out_dir, f"{name}_{runid}_equity.png")
+    hist_path = os.path.join(out_dir, f"{name}_{runid}_hist.png")
+    ric_path = os.path.join(out_dir, f"{name}_{runid}_rolling_ic.png")
@@
-    eq_fig.savefig(eq_path); hist_fig.savefig(hist_path); ric_fig.savefig(ric_path)
+    eq_fig.savefig(eq_path); hist_fig.savefig(hist_path); ric_fig.savefig(ric_path)
@@
-    md_path = os.path.join(out_dir, f"{name}_{datetime.now().strftime('%Y%m%d')}.md")
-    with open(md_path, "w") as f:
-        f.write(md)
+    from utils.atomic import atomic_write_text
+    md_path = os.path.join(out_dir, f"{name}_{runid}_report.md")
+    atomic_write_text(md_path, md)

3) Pinned environment (requirements.lock)

Problem: nieprzewidywalne upgrady łamią pipeline „w środku tygodnia”.
Fix: dodaj lock i procedurę deployu.

Patch (dokumentacja i wsparcie):
*** Begin Patch
*** Update File: README.md
@@
 ## Quick start
@@
-pip install -r requirements.txt
+pip install -r requirements.lock  # produkcja
+# (dev): pip install -r requirements.txt
@@
 ## Cron (Europe/Warsaw)
*** End Patch
*** Add File: scripts/lock_env.md
+### Jak odświeżyć lock plik (dev branch)
+1. Użyj virtualenv:
+   ```bash
+   python -m venv .venv && source .venv/bin/activate
+   pip install -r requirements.txt
+   pip freeze | sed '/^-e /d' > requirements.lock
+   ```
+2. Odpal smoke tests i rerun historyczny (snapshot-replay).
+3. Zmerguj do `main` tylko po review.
*** End Patch


Uwaga: utrzymuj prod na requirements.lock, a dev na requirements.txt.

4) Orders throttle / ratelimit + greylista

Problem: zbyt dużo/duże zlecenia jednorazowo → koszty i ryzyko operacyjne.
Fix: mechanizm limitów na liczbę zleceń, notional per batch i per symbol, z greylistą.

Patch
*** Begin Patch
*** Add File: utils/order_throttle.py
+import pandas as pd
+def apply_throttle(plan: pd.DataFrame, max_orders: int=100, max_notional: float=1_000_000, max_per_symbol: float=100_000, greylist: set[str]|None=None) -> pd.DataFrame:
+    p = plan.copy()
+    if greylist:
+        p = p[~p["symbol"].isin(greylist)]
+    # per-symbol clamp
+    p["qty_usd"] = p.groupby("symbol")["qty_usd"].transform(lambda s: s.clip(upper=max_per_symbol/ max(1, len(s))))
+    # global clamp
+    p = p.sort_values("qty_usd", ascending=False).head(max_orders)
+    if p["qty_usd"].sum() > max_notional:
+        scale = max_notional / p["qty_usd"].sum()
+        p["qty_usd"] *= scale
+    return p.sort_values(["slice_time","symbol"])
*** End Patch


Użycie po wygenerowaniu orders_plan (gdzie go tworzysz):

@@
-    plan = plan_twap(delta_w, prices, adv, spr, equity, window_minutes=60, slices=6, adv_cap=cfg["impact"]["adv_cap_pct"], gamma=cfg["impact"].get("gamma",0.25))
+    plan = plan_twap(delta_w, prices, adv, spr, equity, window_minutes=60, slices=6, adv_cap=cfg["impact"]["adv_cap_pct"], gamma=cfg["impact"].get("gamma",0.25))
+    from utils.order_throttle import apply_throttle
+    grey = set(cfg.get("trading",{}).get("greylist", []))
+    lims = cfg.get("trading",{}).get("limits", {"max_orders":100,"max_notional":1_000_000,"max_per_symbol":100_000})
+    plan = apply_throttle(plan, max_orders=lims["max_orders"], max_notional=lims["max_notional"], max_per_symbol=lims["max_per_symbol"], greylist=grey)


Dodaj w configu:

trading:
  greylist: []
  limits:
    max_orders: 100
    max_notional: 1000000
    max_per_symbol: 100000

5) Cancel-stale policy (safe-mode / kill-switch)

Problem: po kill-switchu stare, niewykonane zlecenia wiszą i mogą „wrócić”.
Fix: generujemy listę anulowań dla poprzedniego planu (po run_id), gdy system wejdzie w safe mode.

Patch
*** Begin Patch
*** Add File: utils/orders_state.py
+import os, json, pandas as pd
+def save_plan(plan: pd.DataFrame, run_id: str, out_dir: str="backtests/results/orders"):
+    os.makedirs(out_dir, exist_ok=True)
+    p = os.path.join(out_dir, f"plan_{run_id}.csv")
+    plan.to_csv(p, index=False)
+    with open(os.path.join(out_dir, "last_run_id.txt"), "w") as f:
+        f.write(run_id+"\n")
+    return p
+
+def cancel_file_for_last_run(out_dir: str="backtests/results/orders") -> str|None:
+    last = os.path.join(out_dir, "last_run_id.txt")
+    if not os.path.exists(last): return None
+    runid = open(last).read().strip()
+    plan_path = os.path.join(out_dir, f"plan_{runid}.csv")
+    if not os.path.exists(plan_path): return None
+    df = pd.read_csv(plan_path)
+    df["action"] = "CANCEL"
+    cancel_path = os.path.join(out_dir, f"cancel_{runid}.csv")
+    df[["symbol","side","qty_usd","action"]].to_csv(cancel_path, index=False)
+    return cancel_path
*** End Patch


W report_daily.py przy kill-switchu:

@@
-    if ks_tripped:
+    if ks_tripped:
         post_message(os.environ.get("SLACK_CHANNEL", "#alpha-lab"),
                      ":no_entry: KILL-SWITCH: przekroczono hard drawdown — zamrażam zmiany wag.")
+        from utils.orders_state import cancel_file_for_last_run
+        cpath = cancel_file_for_last_run()
+        if cpath:
+            post_message(os.environ.get("SLACK_CHANNEL","#alpha-lab"), f"Generuję anulacje pending orders: {os.path.basename(cpath)}")
         weights = weights.shift(1).fillna(0.0)

6) Delta-to-plan loop (target – broker_state)

Problem: plan oderwany od stanu konta → dryf pozycji.
Fix: plan liczymy zawsze z różnicy target-weights vs realne pozycje z brokera (paper/live).

Patch
*** Begin Patch
*** Add File: utils/diff_planner.py
+import pandas as pd
+def delta_from_positions(target_w: pd.Series, broker_qty: pd.Series, last_px: pd.Series, equity: float) -> pd.Series:
+    target_$ = (target_w * equity).reindex(last_px.index).fillna(0.0)
+    broker_$ = (broker_qty.reindex(last_px.index).fillna(0.0) * last_px)
+    delta_$  = (target_$ - broker_$)
+    return delta_$
*** End Patch


W miejscu tworzenia planu (po rekonsyliacji lub bezpośrednio w raporcie):

@@
-    delta_w = weights.iloc[-1] - weights.iloc[-2]
+    # preferowane: delta względem realnego stanu brokera (CSV/API)
+    from utils.diff_planner import delta_from_positions
+    # załaduj broker positions (CSV) jeśli dostępny; fallback do Δw
+    broker_qty = None
+    broker_csv = cfg.get("trading",{}).get("broker_positions_csv")
+    if broker_csv and os.path.exists(broker_csv):
+        bdf = pd.read_csv(broker_csv).set_index("symbol")["quantity"].astype(float)
+        last_px = prices.iloc[-1]
+        delta_$ = delta_from_positions(weights.iloc[-1], bdf, last_px, bt_cfg.get("init_equity",100_000))
+    else:
+        delta_$ = ((weights.iloc[-1] - weights.iloc[-2]) * bt_cfg.get("init_equity",100_000) * prices.iloc[-1]).fillna(0.0)


Podmień w plan_twap(...) argument dollars_traded na delta_$ (lub rozbij na BUY/SELL).

7) Weekly cost calibration (k, γ) z realized slippage

Problem: model kosztów nie uczy się z wyników papierowych.
Fix: prosty kalibrator tygodniowy: zbiera fills.csv (paper), liczy średni slippage vs. mid i aktualizuje rekomendacje k_bps (impact) i gamma (limit band).

Patch
*** Begin Patch
*** Add File: utils/calibration.py
+import os, json, pandas as pd, numpy as np
+def calibrate_costs(fills_csv: str, out_json: str="backtests/results/calibration.json", ew_halflife_days:int=14):
+    if not os.path.exists(fills_csv): return None
+    df = pd.read_csv(fills_csv, parse_dates=["ts"])
+    # oczekujemy kolumn: ts, symbol, side, exec_price, mid_price, adv_usd, notional
+    df["slip_frac"] = (df["exec_price"] - df["mid_price"]) / df["mid_price"] * np.where(df["side"].str.upper()=="BUY", 1, -1)
+    # Estymacja k_bps względem sqrt(notional/ADV)
+    ratio = (df["notional"] / df["adv_usd"]).clip(lower=1e-9)
+    x = np.sqrt(ratio)
+    k_est = (df["slip_frac"].mean() / x.mean()) * 1e4  # bps
+    # Estymacja gamma ~ średnia |slip| / spread
+    if "spread_frac" in df.columns:
+        gamma_est = (df["slip_frac"].abs() / (df["spread_frac"]/2).replace(0,np.nan)).median()
+    else:
+        gamma_est = np.nan
+    rec = {"k_bps_recommended": float(max(1.0, k_est)), "gamma_recommended": None if np.isnan(gamma_est) else float(min(max(gamma_est,0.1),1.0))}
+    os.makedirs(os.path.dirname(out_json), exist_ok=True)
+    with open(out_json,"w") as f: json.dump(rec, f)
+    return rec
*** End Patch


Wywołanie na końcu report_daily.py (po Slacku albo przed):

@@
+    # Tygodniowa kalibracja kosztów (jeśli mamy fills z paper)
+    calibr = cfg.get("impact",{}).get("calibration",{})
+    if calibr.get("fills_csv"):
+        from utils.calibration import calibrate_costs
+        rec = calibrate_costs(calibr["fills_csv"])
+        if rec:
+            post_message(os.environ.get("SLACK_CHANNEL","#alpha-lab"), f"Rekomendacja kosztów: k≈{rec['k_bps_recommended']:.1f} bps; gamma≈{rec.get('gamma_recommended','n/a')}")


Config:

impact:
  calibration:
    fills_csv: backtests/results/paper_fills.csv

8) Two-man rule + audit (prod-guard)

Problem: konfiguracje wchodzą do produkcji bez review.
Fix: prosty guard: w trybie prod (ALPHA_PROD=1) wymagaj, by cfg_hash widniał w backtests/results/approvals.json z ≥2 różnymi „approverami”.

Patch
*** Begin Patch
*** Add File: utils/approval.py
+import os, json
+def enforce_two_man(cfg_hash: str, approvals_path: str="backtests/results/approvals.json", required: int=2):
+    if not os.environ.get("ALPHA_PROD"): 
+        return True
+    if not os.path.exists(approvals_path):
+        raise RuntimeError("Two-man rule: approvals.json missing")
+    data = json.load(open(approvals_path))
+    arr = data.get(cfg_hash, [])
+    if len(set(arr)) < required:
+        raise RuntimeError(f"Two-man rule: config {cfg_hash[:8]} not approved (have {len(set(arr))}/{required})")
+    return True
*** End Patch


Wywołanie na starcie main() (po wczytaniu cfg i policzeniu cfg_hash):

@@
-    with open(cfg_path, "r") as f:
-        cfg = yaml.safe_load(f)
+    with open(cfg_path, "r") as f:
+        cfg = yaml.safe_load(f)
+    from utils.ledger import cfg_hash as _cfg_hash
+    from utils.approval import enforce_two_man
+    enforce_two_man(_cfg_hash(cfg))


Jak approve’ować (manualnie, PR):

// backtests/results/approvals.json
{
  "a1b2c3d4e5...": ["alice","bob"],
  "deadbeef12...": ["alice","carol"]
}
