0) IMPORTY — dodaj na górze pliku

Kotwica do znalezienia: znajdź istniejący blok import ... / from utils... import ...

# INSERT HERE — IMPORTS (dodaj, nie usuwaj istniejących)
import os, sys, io, time
from datetime import datetime

# utils (nowe moduły)
from utils.data_io import load_ohlcv
from utils.freshness import assert_fresh
from utils.snapshot import save_snapshot
from utils.atomic import atomic_write_text
from utils.run_id import make_run_id
from utils.ledger import cfg_hash as _cfg_hash
from utils.approval import enforce_two_man
from utils.execution import rebalance_signal, apply_rebalance_weights
from utils.risk_limits import (
    enforce_gross_limit, enforce_single_name_limit, limit_turnover, kill_switch_by_drawdown
)
from utils.metrics import (
    sharpe, sortino, calmar, max_drawdown, information_ratio, alpha_beta,
    rank_ic, rolling_ic, expected_shortfall, tail_ratio, hit_rate, payoff_ratio,
    skew as mskew, kurtosis as mkurt, deflated_sharpe_ratio
)
from utils.plotting import equity_curve, histogram_returns, rolling_ic_plot
from utils.calendar import compute_forward_returns_sessions
from utils.impact import (
    compute_adv_usd, square_root_impact_cost, capacity_violations,
    corwin_schultz_spread, spread_cost_fraction
)
from utils.order_throttle import apply_throttle
from utils.orders_state import save_plan, cancel_file_for_last_run
from utils.diff_planner import delta_from_positions
from utils.monitor import SLO, alert_thresholds
from utils.runlock import run_lock

# Slack
from utils.slack_client import post_message, upload_file

1) POCZĄTEK main() — run-lock, wczytanie configu, two-man rule

Kotwica: sygnatura def main( i pierwsze linie wewnątrz.

# INSERT HERE — POCZĄTEK main()
def main(cfg_path: str, mode: str = "shadow"):
    t0 = time.time()
    with run_lock():  # anty-duplikacja uruchomień
        with open(cfg_path, "r") as f:
            cfg = yaml.safe_load(f)

        # Two-man rule (prod guard)
        enforce_two_man(_cfg_hash(cfg))

2) PO ZAŁADOWANIU DANYCH — freshness-gate, snapshot, run_id

Kotwica: linia, w której ładujesz dane rynkowe (zastąp stare load_prices*).

# INSERT HERE — LOAD + FRESHNESS + SNAPSHOT + RUN_ID
uni = cfg["universe"]
ds = cfg["data"]
px, vol, hi, lo = load_ohlcv(
    uni, ds.get("start"), ds.get("end"),
    ds.get("source", "yfinance"), ds.get("csv_dir", "data")
)

# Freshness gate (fail-closed)
exp, cov, ok = assert_fresh(px, cfg.get("calendar", {}).get("exchange", "XNYS"),
                            min_coverage=cfg.get("data", {}).get("min_coverage", 0.95))
if not ok:
    try:
        post_message(os.environ.get("SLACK_CHANNEL", "#alpha-lab"),
                     f":no_entry: Freshness gate FAIL — oczekiwana sesja {exp.date()} pokryta w {cov:.1%}. Przerywam run.")
    finally:
        sys.exit(4)

# Snapshot wejścia + run_id
snap_meta = save_snapshot(px, vol, hi, lo)
runid = make_run_id(_cfg_hash(cfg), snap_meta["snapshot_id"])

3) RANGI/FAKTOR, FORWARD W SESJACH, REBALANS — (kotwice „ranks”, „compute_forward…”, „weights”)

Kotwice: tam, gdzie liczysz faktor/ranki, forward returns i wagi.

# INSERT HERE — FORWARD (N sesji)
cal = cfg.get("calendar", {"exchange": "XNYS", "forward_horizon_sessions": 21})
fwd = compute_forward_returns_sessions(px, cal.get("forward_horizon_sessions", 21), cal.get("exchange", "XNYS"))

# ... (tu liczysz faktor i ranks jak dotąd) ...
# ranks = mod.percentile_rank(fac)

# INSERT HERE — REBALANS + ELIGIBILITY T-1
# (przyjmuję, że masz już eligibility mask per date)
flags = rebalance_signal(px.index, cfg["backtest"].get("rebalance", "M"))
elig_tminus1 = elig.shift(1).fillna(False)  # maska handlowa z dnia poprzedniego
weights = apply_rebalance_weights(
    ranks=ranks,
    top_q=cfg["backtest"]["top_quantile"],
    bottom_q=cfg["backtest"]["bottom_quantile"],
    long_only=cfg["backtest"]["long_only"],
    max_weight=cfg["backtest"]["max_weight"],
    cash_buffer=cfg["backtest"]["cash_buffer"],
    rebalance_flags=flags,
    eligibility_prev=elig_tminus1
)

4) HARD LIMITS (gross, single-name, turnover) PRZED LICZENIEM ZWROTÓW

Kotwica: zaraz po wyliczeniu weights.

# INSERT HERE — HARD LIMITS
rl = cfg.get("risk", {})
weights = enforce_single_name_limit(weights, rl.get("max_name_weight", 0.15))
weights = enforce_gross_limit(weights, rl.get("max_gross", 1.5))
if rl.get("max_turnover") is not None:
    weights = limit_turnover(weights, rl["max_turnover"])

5) ZWROTY PORTFELA, KOSZTY (bps + spread + impact)

Kotwica: sekcja liczenia zwrotów i kosztów.

# INSERT HERE — PORTFEL i KOSZTY
ret = px.pct_change().reindex_like(weights).fillna(0.0)
port_ret = (weights.shift(1).fillna(0.0) * ret).sum(axis=1)

# Turnover i Δw
from utils.metrics import turnover as turnover_metric
turnover = turnover_metric(weights)
dw = weights.diff().fillna(weights.iloc[0])

# Impact & capacity
equity = cfg["backtest"].get("init_equity", 100_000)
adv = compute_adv_usd(px, vol, lookback=cfg["impact"].get("lookback_days", 60))
dollars_traded = (dw.abs() * equity * px)
impact_frac = square_root_impact_cost(dollars_traded, adv, k_bps=cfg["impact"].get("k_bps", 15.0))
viol = capacity_violations(dollars_traded, adv, cap_pct=cfg["impact"].get("adv_cap_pct", 0.10))

# Spread proxy (Corwin–Schultz)
spr = corwin_schultz_spread(hi, lo, window=2)
spread_frac = spread_cost_fraction(dw, spr)

# Liniowe koszty (fees+slippage) w bps
from utils.costs import bps_to_frac
lin_cost = turnover * bps_to_frac(cfg["backtest"]["fees_bps"] + cfg["backtest"]["slippage_bps"])

# PnL po kosztach
total_cost = (lin_cost + spread_frac + impact_frac).reindex_like(port_ret).fillna(0.0)
port_ret_after_cost = port_ret - total_cost

6) KILL-SWITCH + CANCEL STALE (przed generowaniem planu / raportu)

Kotwica: tuż po policzeniu port_ret_after_cost albo „na sucho” po port_ret.

# INSERT HERE — KILL SWITCH
ks_cfg = cfg.get("risk", {}).get("kill_switch", {"dd_hard": 0.20})
ks_tripped = kill_switch_by_drawdown(port_ret_after_cost, ks_cfg.get("dd_hard", 0.20))
if ks_tripped:
    post_message(os.environ.get("SLACK_CHANNEL", "#alpha-lab"),
                 ":no_entry: KILL-SWITCH: przekroczono hard drawdown — zamrażam zmiany wag.")
    cpath = cancel_file_for_last_run()
    if cpath:
        post_message(os.environ.get("SLACK_CHANNEL", "#alpha-lab"),
                     f"Generuję anulacje pending orders: {os.path.basename(cpath)}")
    weights = weights.shift(1).fillna(0.0)

7) PLAN ZLECEŃ: DELTA-TO-PLAN → THROTTLE → SAVE

Kotwica: sekcja tworzenia planu zleceń (jeśli nie było — wstaw przed raportem).

# INSERT HERE — DELTA → PLAN TWAP → THROTTLE → SAVE
# a) delta względem stanu brokera (jeśli mamy CSV), fallback do Δw
broker_csv = cfg.get("trading", {}).get("broker_positions_csv")
if broker_csv and os.path.exists(broker_csv):
    bdf = pd.read_csv(broker_csv).set_index("symbol")["quantity"].astype(float)
    delta_dollars = delta_from_positions(weights.iloc[-1], bdf, px.iloc[-1], equity)
else:
    delta_dollars = ((weights.iloc[-1] - weights.iloc[-2]) * equity * px.iloc[-1]).fillna(0.0)

# b) plan TWAP (użyj swojej implementacji plan_twap)
from utils.exec_policy import plan_twap  # jeśli moduł nazwałeś inaczej, dostosuj
plan = plan_twap(
    delta_w= (delta_dollars / (equity * px.iloc[-1])).fillna(0.0),  # względnie, jeśli funkcja wymaga Δw
    prices=px, adv_usd=adv, spread=spr, equity=equity,
    window_minutes=60, slices=6,
    adv_cap=cfg["impact"]["adv_cap_pct"], gamma=cfg["impact"].get("gamma", 0.25)
)

# c) throttle/ratelimit + greylist
grey = set(cfg.get("trading", {}).get("greylist", []))
lims = cfg.get("trading", {}).get("limits", {"max_orders": 100, "max_notional": 1_000_000, "max_per_symbol": 100_000})
plan = apply_throttle(plan, max_orders=lims["max_orders"], max_notional=lims["max_notional"],
                      max_per_symbol=lims["max_per_symbol"], greylist=grey)

# d) zapisz plan (z run_id) — atomika zapewnia CSV writer, ale trzymaj się jednego miejsca
orders_dir = "backtests/results/orders"
plan_path = save_plan(plan, runid, out_dir=orders_dir)


Jeśli masz inną sygnaturę plan_twap, dopasuj argumenty — kluczowe jest, by do throttlingu trafiła kolumna qty_usd.

8) WYKRESY + RAPORT MD — pliki z run_id i zapis atomowy

Kotwica: miejsce, gdzie generujesz PNG/MD i zapisujesz do report/out_dir.

# INSERT HERE — WYKRESY
eq_fig = equity_curve(port_ret_after_cost)
hist_fig = histogram_returns(port_ret_after_cost)
# Rolling IC (przygotuj factor/fwd w wersji „stacked”, jeśli jeszcze tego nie zrobiłeś)
# ric_series = rolling_ic(fac_stack, fwd_stack, window=63)  # albo wylicz inaczej, jeśli masz
ric_fig = rolling_ic_plot(ric_series, title="Rolling IC (63 sesje)")

out_dir = cfg.get("report", {}).get("out_dir", "backtests/reports")
os.makedirs(out_dir, exist_ok=True)
eq_path   = os.path.join(out_dir, f"{cfg['name']}_{runid}_equity.png")
hist_path = os.path.join(out_dir, f"{cfg['name']}_{runid}_hist.png")
ric_path  = os.path.join(out_dir, f"{cfg['name']}_{runid}_rolling_ic.png")
eq_fig.savefig(eq_path); hist_fig.savefig(hist_path); ric_fig.savefig(ric_path)

# INSERT HERE — RAPORT .md z atomowym zapisem
now = datetime.utcnow().strftime("%Y-%m-%d %H:%M UTC")
header = (
    f"# {cfg.get('report',{}).get('title','Raport dzienny')}\n\n"
    f"*Run ID:* `{runid}`  \\\n"
    f"*Snapshot:* `{snap_meta['snapshot_id']}`  \\\n"
    f"*Data generacji:* {now}\n"
)
# ...zbuduj body: metryki, alerty, capacity, itp...
md_text = header + "\n" + body  # body = Twoja sekcja tabel/wniosków
md_path = os.path.join(out_dir, f"{cfg['name']}_{runid}_report.md")
atomic_write_text(md_path, md_text)

9) SLO + ALERTY + SLACK (po policzeniu metryk i zrobieniu wykresów)

Kotwica: sekcja wysyłki do Slacka.

# INSERT HERE — SLO i Slack
ic3 = ric_series.tail(63).mean() if ric_series.notna().any() else float("nan")
ic12 = ric_series.tail(252).mean() if ric_series.notna().any() else float("nan")
slo = SLO(
    step_times={"total": time.time()-t0},
    n_assets_after_filter=weights.columns.size,
    pct_missing_today=float(px.iloc[-1].isna().mean()),
    ic_3m=float(ic3) if ic3==ic3 else float("nan"),
    ic_12m=float(ic12) if ic12==ic12 else float("nan"),
    adv_viol_share=float(viol.reindex(port_ret.index).fillna(0.0).iloc[-1]) if 'viol' in locals() else 0.0
)
slo_alerts = alert_thresholds(slo)

summary = f"**{cfg.get('report',{}).get('title','Raport')}** — {now}\nRun: `{runid}`  •  Snapshot: `{snap_meta['snapshot_id']}`"
triggered = []
if ks_tripped: triggered.append("KILL-SWITCH")
if not slo_alerts:
    post_message(os.environ.get("SLACK_CHANNEL", "#alpha-lab"), summary)
else:
    post_message(os.environ.get("SLACK_CHANNEL", "#alpha-lab"),
                 ":rotating_light: " + "; ".join(slo_alerts) + "\n" + summary)

# upload plików
upload_file(os.environ.get("SLACK_CHANNEL", "#alpha-lab"), eq_path,   os.path.basename(eq_path),   "Equity")
upload_file(os.environ.get("SLACK_CHANNEL", "#alpha-lab"), hist_path, os.path.basename(hist_path), "Histogram")
upload_file(os.environ.get("SLACK_CHANNEL", "#alpha-lab"), ric_path,  os.path.basename(ric_path),  "Rolling IC")
upload_file(os.environ.get("SLACK_CHANNEL", "#alpha-lab"), md_path,   os.path.basename(md_path),   "Raport .md")

10) TYGODNIOWA KALIBRACJA KOSZTÓW (na końcu main())

Kotwica: koniec funkcji, przed return/if __name__ == "__main__":

# INSERT HERE — KALIBRACJA K/γ Z PAPER FILLS
calibr = cfg.get("impact", {}).get("calibration", {})
if calibr.get("fills_csv"):
    from utils.calibration import calibrate_costs
    rec = calibrate_costs(calibr["fills_csv"])
    if rec:
        post_message(os.environ.get("SLACK_CHANNEL", "#alpha-lab"),
                     f"Rekomendacja kosztów (paper): k≈{rec['k_bps_recommended']:.1f} bps; gamma≈{rec.get('gamma_recommended','n/a')}")

Wzorce CSV (schematy)
1) broker_positions.csv

Kolumny:

symbol (str),

quantity (float; ujemne dla short),

(opcjonalnie) asof (ISO), account.

symbol,quantity,asof,account
AAPL,120,2025-10-10T20:05:00Z,DU1234567
MSFT,-50,2025-10-10T20:05:00Z,DU1234567

2) paper_fills.csv (do kalibracji kosztów)

Kolumny (minimum):

ts (ISO), symbol, side (BUY/SELL),

exec_price (float), mid_price (float),

notional (float, USD), adv_usd (float),

(opcjonalnie) spread_frac (np. 0.002 = 20 bps).

ts,symbol,side,exec_price,mid_price,notional,adv_usd,spread_frac
2025-10-10T21:05:00Z,AAPL,BUY,195.12,195.05,25000,32000000,0.0018
2025-10-10T21:07:00Z,MSFT,SELL,408.55,408.62,18000,41000000,0.0015

3) target_weights.csv (dla skryptu rekonsyliacji, jeśli używasz)

Kolumny:

symbol, weight (float), last_price (float)

symbol,weight,last_price
AAPL,0.10,195.05
MSFT,-0.05,408.62

Config — dopiski (jeśli jeszcze nie masz)
data:
  source: yfinance
  csv_dir: data
  start: 2015-01-01
  min_coverage: 0.95

impact:
  enabled: true
  k_bps: 15.0
  adv_cap_pct: 0.10
  lookback_days: 60
  gamma: 0.25
  calibration:
    fills_csv: backtests/results/paper_fills.csv

trading:
  broker_positions_csv: backtests/results/broker_positions.csv
  greylist: []
  limits:
    max_orders: 100
    max_notional: 1000000
    max_per_symbol: 100000

risk:
  max_name_weight: 0.15
  max_gross: 1.5
  max_turnover: 0.30
  kill_switch: { dd_hard: 0.20 }

report:
  out_dir: backtests/reports
  title: "Raport dzienny – Momentum 12–1 (pro)"